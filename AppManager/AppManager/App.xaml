<Application x:Class="AppManager.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	 xmlns:am="clr-namespace:AppManager">
	<Application.Resources>
		<!--<ContextMenu x:Key="TrayMenu" StaysOpen="true">
			<MenuItem Name="Show" Header="{Binding Source={x:Static am:Strings.MNU_SHOW}}"/>
			<MenuItem Header="{Binding Source={x:Static am:Strings.MNU_HIDE}}"/>
			<Separator/>
			<MenuItem Header="{Binding Source={x:Static am:Strings.SETTINGS}}"/>
			<MenuItem Header="{Binding Source={x:Static am:Strings.MNU_MANAGEAPP}}"/>
			<MenuItem Header="{Binding Source={x:Static am:Strings.MNU_CLOSE}}"/>
		</ContextMenu>-->
		<ContextMenu x:Key="ItemMenu">
			<MenuItem Name="Rename" Header="{Binding Source={x:Static am:Strings.MNU_SHOW}}"/>
			<MenuItem Name="Delete" Header="{Binding Source={x:Static am:Strings.MNU_HIDE}}"/>
			<MenuItem Name="Cancel" Header="{Binding Source={x:Static am:Strings.SETTINGS}}"/>
		</ContextMenu>
	</Application.Resources>
</Application>
